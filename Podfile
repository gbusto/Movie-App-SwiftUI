# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Movie app SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Movie app SwiftUI
  pod 'Kingfisher', '~> 7.0'
  pod 'Alamofire'

  target 'Movie app SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Movie app SwiftUIUITests' do
    # Pods for testing
  end

end
